#include <stdio.h>
/**
 * main -a program that prints alphabets in lowercase
 *
 * Return: (0) (successful)
 */
int main(void)
{
	int n = 97;

	while (n < 122)
	{
		putchar(n);
		n++;
	}
		putchar(\n);

	return (0);
}
